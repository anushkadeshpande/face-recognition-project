import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sharinushka.facerec',
  appName: 'Face Recognition',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
